## Rust library for using Telegram MTProto with the main focus on stability.

Warning: some modules may be subject to change.

Run test program yourself! (send `req_pq_multi#be7e8ef1` and recv `ResPQ`)
```shell
cargo run --bin hungry-test
```

Once this project is complete and fully refactored, the repository will be recreated. ⚠️
